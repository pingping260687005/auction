import { FilterValuePipe } from './filter-value.pipe';

describe('FilterValuePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterValuePipe();
    expect(pipe).toBeTruthy();
  });
});
